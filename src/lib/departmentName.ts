/** Max length for department names in the UI and proxy validation (DB column is VARCHAR(255)). */
export const DEPARTMENT_NAME_MAX_LENGTH = 100;

/** Max length for department description in the UI (backend may allow more). */
export const DEPARTMENT_DESCRIPTION_MAX_LENGTH = 2000;
